> Spiegelkopie der Memory-Datei `~/.claude/projects/E--Simone/memory/user-web-stack.md` – damit jede Claude-Instanz sie direkt im Repo lesen kann.

---
name: user-web-stack
description: Josh's preferred web stack and tooling (Astro + TypeScript + PostCSS, GSAP, npm, gh CLI, Cloudflare) inferred from his portfolio and other repos
metadata:
  type: user
---

Josh (joshuabinswanger on GitHub, scientific illustrator / 3D designer, works at Augmedi) builds websites with **Astro + TypeScript + plain CSS/PostCSS** (no Tailwind), uses **GSAP** for animation, **npm** (portfolio) or **pnpm** (AugMedi monorepo), and **React** in the monorepo. His portfolio rhizome.ch is an Astro site. He has the `gh` CLI authenticated, `ffmpeg`, Python 3.14 with PyMuPDF, and `uv`. He has used Cloudflare (wrangler, D1) in the "Science Digest" project but no deploy CLIs (vercel/netlify/wrangler) are installed globally.

**How to apply:** For new web projects default to Astro + TS + PostCSS/plain CSS, GitHub repo via `gh`, free static hosting (GitHub Pages or Cloudflare Pages). See [[simone-website-project]].
